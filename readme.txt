In order to redo the database, you'll need to create a file named "secret.py" and add your glassnode api key as follows:
glassnode = 'YOUR_API_KEY'
Otherwise you don't need and you should only execute sql.py